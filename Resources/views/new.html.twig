{% extends 'VictoireCoreBundle:Widget:new.html.twig' %}

{% block form_static %}
    {{ form_start(form) }}
        {{ form_widget(form.content) }}
        {{ form_rest(form) }}
    {{ form_end(form) }}

    <script type="text/javascript">
        $vic(document).ready(function(){
            initRedactor('.redactor');
        });
    </script>
{% endblock form_static %}
